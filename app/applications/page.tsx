"use client";
import Link from "next/link";import{useEffect,useMemo,useState}from"react";import{supabase}from"@/lib/supabase";import type{Application,ApplicationStatus}from"@/types/application";
const tabs=["all","ready","sent","replied","interview","closed"] as const;
export default function ApplicationsPage(){const[rows,setRows]=useState<Application[]>([]),[tab,setTab]=useState<(typeof tabs)[number]>("all");useEffect(()=>{void supabase.from("applications").select("*").order("updated_at",{ascending:false}).then(({data})=>setRows((data??[])as Application[]))},[]);const shown=useMemo(()=>tab==="all"?rows:rows.filter(x=>x.application_status===tab),[rows,tab]);return <main className="mx-auto max-w-6xl px-6 py-14"><header className="border-b-2 border-black pb-8"><p className="text-xs tracking-[.25em]">APPLICATION PIPELINE</p><h1 className="mt-3 text-5xl font-light">投递记录</h1></header><nav className="my-8 flex flex-wrap gap-2">{tabs.map(x=><button key={x} onClick={()=>setTab(x)} className={tab===x?"bg-black px-4 py-2 text-white":"border px-4 py-2"}>{x.toUpperCase()}</button>)}</nav><div className="overflow-x-auto"><table className="w-full text-left"><thead><tr className="border-b border-black text-xs tracking-widest"><th className="py-3">COMPANY</th><th>ROLE</th><th>MATCH</th><th>RESUME</th><th>RECRUITER</th><th>STATUS</th><th>APPLIED</th></tr></thead><tbody>{shown.map(x=><tr key={x.id} className="border-b border-neutral-300"><td className="py-5"><Link className="underline" href={`/applications/${x.id}`}>{x.company_name}</Link></td><td>{x.job_title}</td><td>{x.match_score_snapshot??"—"}%</td><td>{x.resume_version_id?"Ready":"Missing"}</td><td>{x.recruiter_name||"—"}</td><td>{x.application_status}</td><td>{x.applied_at?new Date(x.applied_at).toLocaleDateString("zh-CN"):"—"}</td></tr>)}</tbody></table></div>{!shown.length&&<p className="py-16 text-neutral-500">暂无记录。请从岗位详情准备一次投递。</p>}</main>}

