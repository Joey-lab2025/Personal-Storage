"use client";import Link from"next/link";import{useEffect,useState}from"react";import{supabase}from"@/lib/supabase";import type{ResumeVersion}from"@/types/job";
export default function Resumes(){const[items,setItems]=useState<ResumeVersion[]>([]);useEffect(()=>{void supabase.from("resume_versions").select("*").order("created_at",{ascending:false}).then(({data})=>setItems((data??[])as ResumeVersion[]))},[]);return <main className="mx-auto min-h-screen max-w-5xl px-6 py-16"><div className="border-b border-black pb-6"><p className="text-xs tracking-[.2em] text-neutral-500">JOB AGENT</p><h1 className="mt-2 text-5xl font-light">简历版本</h1></div><div className="mt-8">{items.map(item=><Link key={item.id} href={`/resumes/${item.id}`} className="grid gap-3 border-b border-neutral-300 py-6 md:grid-cols-[1fr_1fr_auto]"><b>{item.target_company}</b><span>{item.target_job}</span><span className="text-neutral-500">{new Date(item.created_at).toLocaleDateString("zh-CN")}</span></Link>)}{!items.length&&<p className="py-16 text-neutral-500">还没有定制简历，请先从岗位详情页生成。</p>}</div><nav className="mt-10 flex gap-6"><Link href="/jobs">Jobs</Link><Link href="/">Dashboard</Link></nav></main>}
