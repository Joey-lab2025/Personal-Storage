import{extractBossJob}from"./extractor/boss.js";import{extractVisibleBossCandidates}from"./extractor/bossSearch.js";
chrome.runtime.onMessage.addListener((message,_sender,sendResponse)=>{if(message?.type==="EXTRACT_JOB"){try{sendResponse({ok:true,extraction:extractBossJob()})}catch(error){sendResponse({ok:false,error:error instanceof Error?error.message:"Unable to read this job."})}return true}if(message?.type==="EXTRACT_CANDIDATES"){sendResponse({ok:true,candidates:extractVisibleBossCandidates()});return true}});
let syncTimer:number|undefined;let lastSignature="";
function scheduleSync(){clearTimeout(syncTimer);syncTimer=window.setTimeout(()=>void syncCandidates(),1200)}
async function syncCandidates(){if(!location.pathname.startsWith("/web/geek/job"))return;const candidates=extractVisibleBossCandidates();const signature=candidates.map(item=>item.source_url).sort().join("|");if(!candidates.length||signature===lastSignature)return;lastSignature=signature;const settings=await chrome.storage.local.get(["base_url","token"]);if(typeof settings.base_url!=="string"||typeof settings.token!=="string"||!settings.token)return;try{await fetch(`${settings.base_url.replace(/\/$/,"")}/api/jobs/candidates`,{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${settings.token}`},body:JSON.stringify({candidates})});await chrome.storage.local.set({last_candidate_sync:new Date().toISOString()})}catch{/* A later scroll/navigation retries safely. */}}
if(location.pathname.startsWith("/web/geek/job")){scheduleSync();addEventListener("scroll",scheduleSync,{passive:true});new MutationObserver(scheduleSync).observe(document.body,{childList:true,subtree:true})}
