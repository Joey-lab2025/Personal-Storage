import type{JobCandidate}from"../types/job.js";
const clean=(value:string|undefined|null)=>value?.replace(/\s+/g," ").trim()||"";
const read=(root:Element,selectors:string[])=>{for(const selector of selectors){const value=clean(root.querySelector(selector)?.textContent);if(value)return value}return""};
export function extractVisibleBossCandidates():JobCandidate[]{const cards=[...document.querySelectorAll(".job-card-wrapper,.job-list-box li,.search-job-result li,[class*='job-card']")];const seen=new Set<string>();const results:JobCandidate[]=[];for(const card of cards){const rect=card.getBoundingClientRect();if(rect.bottom<0||rect.top>innerHeight)continue;const anchor=card.querySelector("a[href*='/job_detail/']")as HTMLAnchorElement|null;if(!anchor)continue;const sourceUrl=new URL(anchor.href,location.href).toString();if(seen.has(sourceUrl))continue;seen.add(sourceUrl);results.push({source_url:sourceUrl,source_job_id:sourceUrl.match(/job_detail\/([^./?]+)/)?.[1]||"",company_name:read(card,[".company-name","[class*='company-name']",".company-info h3"]),job_title:read(card,[".job-name","[class*='job-name']",".job-title","h3"]),location:read(card,[".job-area","[class*='job-area']",".job-location"]),salary:read(card,[".salary","[class*='salary']"]),summary:read(card,[".tag-list",".job-card-footer",".job-info","[class*='tag']"])})}return results.filter(item=>item.job_title)}
